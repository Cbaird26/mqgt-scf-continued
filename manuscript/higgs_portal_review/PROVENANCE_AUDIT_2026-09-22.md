# Provenance audit — early review-branch README and claims ledger

**22 September 2026 | Dated documentation repair, not scientific peer review.** Human author: Christopher Michael Baird; ChatGPT (ZoraASI) assisted with an exact Git-blob comparison. This audit concerns the author-approved draft review branch of `Cbaird26/mqgt-scf-continued` only; it does not modify `main` or the archival source PDFs.

## Recoverable source-of-record snapshots

The earlier `manuscript/higgs_portal_review/README.md` survives in Git blob `cb8f88cbbc5e38b2a59232105c8dc0c61726e0a3`. The README currently reviewed in this audit has blob `4adb8ada29b7a3a998f313dbae2cacb228374f6d`; its expanded note-by-note overview is useful but it does not reproduce *every* historical provenance detail. The earlier `manuscript/higgs_portal_review/CLAIMS_AND_RELEASE_GATE.md` survives in Git blob `d66d3e33287e5bea8eb18ca0609d4c011edb601c`. The later ledger at this audit has blob `99faa14f03433d67c21a12d9276f8ea206006e6a`. These are **existing Git objects fetched directly**, not guessed source versions; do not mistake the old snapshots for present-day assessments. To inspect them, retrieve the Git blob objects via `https://api.github.com/repos/Cbaird26/mqgt-scf-continued/git/blobs/<the-exact-SHA>` or the branch's previous commits. This note restores explicit *pointers* to lost context, without pretending the original text was never changed.

## Substantive early details that the newer overviews abbreviate or omit

1. The original September 21 ledger expressly named **six** locally executed checks in `higgs_portal_checks.py`: trace invariance, massless summed width, equal-portal transition, example branching ratios, illustrative coupling norm, and channel thresholds. The original README identified an **aggregate 17 local mathematical tests** across `higgs_portal_checks.py` (6), `h2_audit_checks.py` (5), and `test_research_note_14.py` (6). They were toy/regression checks, **not physics validation**.
2. The original dated Note-21 addendum identified **15 locally passing synthetic-fixture/algebra tests** of `safe_omnes_manifest.py` and its associated suite. The number `0.004138912` was **only** a low-window *kinematic-weight fraction*, never a physical QCD width fraction; a hypothetical 50% low-window width would require a low/high **weighted-mean spectral-strength ratio** approximately `240.6094`, not a measured ratio. The four pinned upstream Omnès files remained unacquired and unparsed.
3. The historical `m1=10 GeV,m2=25 GeV` **15-GeV mass-gap** point had a leptonic-only *proper-distance upper bound* about **17.06 cm**. The later, deliberately **different** compressed `m1=10 GeV,m2=11.5 GeV` **1.5-GeV-gap** point instead has a far weaker leptonic-only `c*tau <= ~645 km` upper bound. They must never be combined as if one benchmark or presented as measured lifetimes.
4. The original ledger required an independently verified portal-vertex/width normalization, precise source edition/page/commit pinning, full vacuum/stability and radiative checks, detector acceptance, appropriate dated likelihoods, a full bibliography and author/AI/licensing/DOI-release decisions. The later ledger retains broader release gates; this historical audit makes the earlier specificity recoverable.

**Source handling:** These are factual statements about what the old Git files *said*, not newly performed laboratory tests or evidence that the field theory is correct. The current reviews and Notes 14–35 must be read as **dated development addenda** to older manuscripts; no silent backport, independent referee, DOI mint, journal submission or merger follows from passing code tests. The new Note 35 supplies only a conditional *leptonic* thermal-average calculation and does not authenticate the hadronic inputs. Keep all of the above gates open until directly verified.